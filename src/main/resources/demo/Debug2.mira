module Debug2;

var point : struct {
    var x;
    var y;
};

var abc : {
    var aaa;
};

var origin : $point{$x : 0, $y : 5};

hello($origin);

pub fn hello(name) {
    println($name.x);

    hello2($name.x);

    return hello $name.x;
}

fn hello2(name) {
    $name;
}
