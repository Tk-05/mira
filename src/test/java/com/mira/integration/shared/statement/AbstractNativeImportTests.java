package com.mira.integration.shared.statement;

// Native imports are interpreter-only — no shared tests.
public abstract class AbstractNativeImportTests {
    protected abstract String runForOutput(String source);
}
