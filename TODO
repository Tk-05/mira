TODO:
    -foreach statements
    -arrays
        -List functions
        -Slices
    -native functions -> files etc.
        -Override native functions
    -Regex
    -Fix access expression : print(append("World", $list)[0]);