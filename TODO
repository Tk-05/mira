TODO:
    -arrays
        -List functions
        -Slices
    -Regex
    -strict var type
    -comparison between chars    
        import io;
        import list;
        import string;

        var file : readFile($args[0]);
        var lines : {};

        for(var i : 0; $i < length(file); $i++){
            if(charAt($i, $file) == "\n") {
                print("new line!");
            }
        }