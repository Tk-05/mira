TODO:
    -arrays
        -List functions
        -Slices
    -Regex
    -strict var type
    -Optimization:
        import string;
        import io;

        fn main() {
            var file : readFile("C:\\Users\\tillk\\Desktop\\vsc\\mira\\src\\main\\java\\com\\mira\\runtime\\interpreter\\Interpreter.java");

            var lines : 0;
            for(var i : 0; $i < length($file); $i++) {
                if(charAt($i, $file) == "\n") {
                    $lines++;
                }
            }

            print($lines++ " lines \n");
        }